# 퀴즈 코어(연습/실제 테스트) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Supabase(RLS + RPC)를 통해 정답이 클라이언트에 노출되지 않는 연습 모드와, 기기당 1회로 제한되는 실제 테스트 모드를 구현한다.

**Architecture:** Next.js(`output: "export"`) 정적 사이트에서 퀴즈 페이지는 전부 클라이언트 컴포넌트로 동작하며, 문제/채점은 빌드에 포함되지 않고 항상 런타임에 Supabase RPC(`SECURITY DEFINER`)를 호출해 처리한다. `quiz_questions` 원본 테이블과 `quiz_attempts` 테이블은 RLS로 anon 접근을 전면 차단하고, 정답이 빠진 뷰와 RPC 함수만 anon에 노출한다.

**Tech Stack:** Next.js 16 / React 19 / TypeScript, Supabase(PostgreSQL, RLS, RPC), Supabase CLI(로컬 개발 + pgTAP), Vitest(프론트 유닛 테스트), `@supabase/supabase-js`, `@fingerprintjs/fingerprintjs`.

**Spec:** [`docs/superpowers/specs/2026-09-04-quiz-feature-design.md`](../specs/2026-09-04-quiz-feature-design.md)

## Global Constraints

- 퀴즈 데이터는 빌드 타임에 정적으로 굽지 않는다 — 항상 런타임에 클라이언트가 Supabase를 호출해서 받아온다 (스펙 §1).
- `quiz_questions`, `quiz_attempts` 원본 테이블에는 `anon`/`authenticated`에 아무 grant도 주지 않는다. 모든 접근은 `SECURITY DEFINER` RPC를 통해서만 이루어진다 (스펙 §3, §4).
- 정답(`correct_answer`)은 `reveal_practice_answer` RPC 응답을 제외한 어떤 API 응답에도 포함되지 않는다 (스펙 §4).
- 실제 테스트 중복 응시 하드 차단 기준은 `device_uuid` 또는 `fingerprint_hash` 매치만 사용한다. IP는 로그 목적으로만 저장하고 차단에는 쓰지 않는다 (스펙 §5).
- 실제 테스트는 활성 문제 풀에서 랜덤 서브셋 10문항을 출제한다 (스펙 §6).
- 이 플랜은 도감 기능을 다루지 않는다(스펙 §9, 범위 밖). 어드민 CRUD/인증은 별도 플랜(`2026-09-04-quiz-admin.md`, 아직 미작성)에서 다룬다.

## 사전 준비 (실행 전 확인)

- Docker Desktop이 설치되어 있고 실행 중이어야 한다 (`supabase start`/`supabase test db`가 로컬 Postgres를 Docker로 띄움). 이 플랜을 실행하는 사람이 직접 Docker Desktop을 켜야 한다.
- Supabase 프로젝트가 이미 생성되어 있고, 프로젝트 URL/anon key/project ref를 가지고 있어야 한다 (Task 1에서 필요).

---

### Task 1: Supabase CLI 로컬 연결 및 환경변수

**Files:**

- Modify: `package.json`
- Create: `.env.local`
- Create: `supabase/` (via `supabase init`)

**Interfaces:**

- Consumes: 없음 (최초 셋업 태스크)
- Produces: `supabase/config.toml`(이후 태스크의 마이그레이션이 참조), `.env.local`의 `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_PUB_KEY`(Task 7에서 소비)

- [ ] **Step 1: Supabase CLI를 devDependency로 추가**

```bash
pnpm add -D supabase
```

`pnpm`은 기본적으로 postinstall 스크립트를 차단하므로, `package.json`의 `pnpm.onlyBuiltDependencies`에 `"supabase"`를 추가해야 CLI 바이너리가 정상 설치된다.

`package.json`의 `pnpm` 필드를 다음과 같이 수정:

```json
  "pnpm": {
    "onlyBuiltDependencies": [
      "@swc/core",
      "esbuild",
      "supabase",
      "unrs-resolver",
      "workerd"
    ]
  }
```

- [ ] **Step 2: 재설치 후 CLI 동작 확인**

```bash
pnpm install
pnpm exec supabase --version
```

Expected: 버전 문자열 출력 (예: `2.x.x`).

- [ ] **Step 3: Supabase 프로젝트 초기화**

```bash
pnpm exec supabase init
```

Expected: `supabase/config.toml`, `supabase/migrations/`, `supabase/tests/` 등이 생성됨. (`init` 실행 시 나오는 VS Code/IntelliJ 확장 추천 프롬프트는 `N`으로 답해도 무방)

- [ ] **Step 4: 환경변수 파일 생성**

`.env.local` 생성 (이미 `.gitignore`의 `.env*` 패턴에 포함되어 git에 커밋되지 않음):

```bash
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_PUB_KEY=<publishable-key>
```

**이 단계는 실행자가 본인의 Supabase 프로젝트 값으로 직접 채워야 한다** — Supabase 대시보드의 Project Settings → API에서 URL과 `publishable`(구 `anon` `public`) key를 복사. (Supabase가 키 명칭을 `anon`/`service_role`에서 `publishable`/`secret`으로 바꾸는 중이라, 프로젝트에 따라 화면 표기가 다를 수 있음 — 둘 다 클라이언트에 노출해도 되는 공개 키라는 점은 동일)

- [ ] **Step 5: Supabase CLI 로그인 및 프로젝트 연결 (수동)**

```bash
pnpm exec supabase login
pnpm exec supabase link --project-ref <project-ref>
```

`login`은 브라우저 인증이 필요해 에이전트가 대신 실행할 수 없다 — 실행자가 직접 터미널에서 로그인/link까지 완료해야 다음 태스크의 `supabase start`/`supabase test db`가 동작한다.

- [ ] **Step 6: 커밋**

```bash
git add package.json pnpm-lock.yaml supabase/config.toml .gitignore
git commit -m "chore: Supabase CLI 로컬 개발 환경 셋업"
```

(`.env.local`은 gitignore 대상이라 커밋 대상에서 제외됨. `supabase init`이 `.gitignore`를 건드렸다면 함께 포함.)

---

### Task 2: 퀴즈 스키마 + RLS (quiz_questions / quiz_questions_public / quiz_attempts)

**Files:**

- Create: `supabase/tests/database/001_quiz_questions_rls.sql`
- Create: `supabase/tests/database/002_quiz_attempts_rls.sql`
- Create: `supabase/migrations/20260904090000_quiz_core_schema.sql`

**Interfaces:**

- Consumes: Task 1의 로컬 Supabase 스택
- Produces: `quiz_questions`, `quiz_questions_public`, `quiz_attempts` 테이블/뷰 — 이후 모든 태스크(RPC, 프론트엔드)가 이 스키마를 전제로 함

- [ ] **Step 1: 실패하는 pgTAP 테스트 작성 (quiz_questions)**

`supabase/tests/database/001_quiz_questions_rls.sql`:

```sql
begin;
select plan(5);

insert into quiz_questions (id, type, question_text, choices, correct_answer, explanation, is_active)
values ('11111111-1111-1111-1111-111111111111', 'ox', 'test question', null, 'X', 'test explanation', true);

set local role anon;

select is(
  (select count(*) from quiz_questions)::int,
  0,
  'anon은 quiz_questions 테이블을 직접 SELECT 할 수 없다'
);

select is(
  (select count(*) from quiz_questions_public)::int,
  1,
  'anon은 quiz_questions_public 뷰는 조회할 수 있다'
);

select hasnt_column(
  'public', 'quiz_questions_public', 'correct_answer',
  'quiz_questions_public 뷰에는 correct_answer 컬럼이 없다'
);

select hasnt_column(
  'public', 'quiz_questions_public', 'explanation',
  'quiz_questions_public 뷰에는 explanation 컬럼이 없다'
);

prepare anon_insert as
  insert into quiz_questions (type, question_text, correct_answer) values ('ox', 'x', 'O');
select throws_ok('anon_insert', '42501', null, 'anon은 quiz_questions에 INSERT 할 수 없다');

select * from finish();
rollback;
```

- [ ] **Step 2: 실패하는 pgTAP 테스트 작성 (quiz_attempts)**

`supabase/tests/database/002_quiz_attempts_rls.sql`:

```sql
begin;
select plan(4);

insert into quiz_questions (id, type, question_text, correct_answer, is_active)
values ('22222222-2222-2222-2222-222222222222', 'ox', 'test', 'O', true);

insert into quiz_attempts (id, device_uuid, question_ids, status, score)
values (
  '33333333-3333-3333-3333-333333333333', 'device-seed',
  '["22222222-2222-2222-2222-222222222222"]'::jsonb, 'in_progress', null
);

set local role anon;

select is(
  (select count(*) from quiz_attempts)::int, 0,
  'anon은 quiz_attempts를 직접 SELECT 할 수 없다'
);

prepare anon_insert_attempt as
  insert into quiz_attempts (device_uuid, question_ids) values ('x', '[]'::jsonb);
select throws_ok(
  'anon_insert_attempt', '42501', null,
  'anon은 quiz_attempts에 직접 INSERT 할 수 없다'
);

update quiz_attempts set score = 999
  where id = '33333333-3333-3333-3333-333333333333';
reset role;
select is(
  (select score from quiz_attempts where id = '33333333-3333-3333-3333-333333333333'),
  null,
  'anon의 UPDATE는 RLS에 막혀 반영되지 않는다'
);

set local role anon;
delete from quiz_attempts where id = '33333333-3333-3333-3333-333333333333';
reset role;
select is(
  (select count(*) from quiz_attempts
   where id = '33333333-3333-3333-3333-333333333333')::int,
  1,
  'anon의 DELETE는 RLS에 막혀 행이 삭제되지 않는다'
);

select * from finish();
rollback;
```

- [ ] **Step 3: 로컬 스택 기동 및 테스트 실행 → 실패 확인**

```bash
pnpm exec supabase start
pnpm exec supabase test db
```

Expected: FAIL — `relation "quiz_questions" does not exist` (테이블이 아직 없으므로).

- [ ] **Step 4: 마이그레이션 작성**

`supabase/migrations/20260904090000_quiz_core_schema.sql`:

```sql
create extension if not exists pgcrypto;

create table quiz_questions (
  id             uuid primary key default gen_random_uuid(),
  type           text not null check (type in ('mc', 'ox')),
  question_text  text not null,
  choices        jsonb,
  correct_answer text not null,
  explanation    text,
  is_active      boolean not null default true,
  created_at     timestamptz not null default now()
);

alter table quiz_questions enable row level security;
-- 의도적으로 정책을 만들지 않음: RLS 활성화 + 정책 없음 = anon/authenticated 전면 차단.
-- 원본 테이블 소유자(마이그레이션 실행 role)는 RLS를 우회하므로 아래 뷰가 정상 동작한다.
-- (FORCE ROW LEVEL SECURITY는 절대 켜지 말 것 — 켜면 뷰까지 막혀버림)

create view quiz_questions_public as
  select id, type, question_text, choices
  from quiz_questions
  where is_active;

grant select on quiz_questions_public to anon;

create table quiz_attempts (
  id               uuid primary key default gen_random_uuid(),
  device_uuid      text not null,
  fingerprint_hash text,
  ip               inet,
  question_ids     jsonb not null,
  answers          jsonb,
  score            int,
  total            int,
  status           text not null default 'in_progress'
                   check (status in ('in_progress', 'submitted')),
  started_at       timestamptz not null default now(),
  submitted_at     timestamptz
);

alter table quiz_attempts enable row level security;
-- 정책 없음 = anon 전면 차단. 모든 접근은 SECURITY DEFINER RPC(Task 4~6)를 통해서만.

create index quiz_attempts_device_uuid_idx on quiz_attempts (device_uuid);
create index quiz_attempts_fingerprint_hash_idx on quiz_attempts (fingerprint_hash);
```

- [ ] **Step 5: 테스트 재실행 → 통과 확인**

```bash
pnpm exec supabase test db
```

Expected: PASS — `001_quiz_questions_rls.sql`, `002_quiz_attempts_rls.sql` 모두 통과.

- [ ] **Step 6: 커밋**

```bash
git add supabase/migrations/20260904090000_quiz_core_schema.sql supabase/tests/database/001_quiz_questions_rls.sql supabase/tests/database/002_quiz_attempts_rls.sql
git commit -m "feat: 퀴즈 스키마 및 RLS 추가 (quiz_questions/quiz_attempts)"
```

---

### Task 3: RPC `get_random_practice_question`

> 스펙(§6)은 "연습 모드는 `quiz_questions_public`에서 무작위 1문제 요청"이라고만 적혀 있었는데,
> PostgREST는 `order` 파라미터로 컬럼명만 받고 `random()` 같은 임의 SQL 함수를 허용하지
> 않는다. 그래서 뷰를 직접 쿼리하는 대신 이 RPC로 무작위 출제를 서버 쪽에서 처리한다 —
> 스펙 의도(정답 비노출)는 동일하게 유지된다.

**Files:**

- Create: `supabase/tests/database/003_get_random_practice_question.sql`
- Create: `supabase/migrations/20260904090100_get_random_practice_question.sql`

**Interfaces:**

- Consumes: Task 2의 `quiz_questions` 테이블
- Produces: RPC `get_random_practice_question() returns jsonb` — `{id, type, question_text, choices}` 형태, `correct_answer`/`explanation` 없음. Task 10의 `getRandomPracticeQuestion()`이 이 RPC를 호출.

- [ ] **Step 1: 실패하는 pgTAP 테스트 작성**

`supabase/tests/database/003_get_random_practice_question.sql`:

```sql
begin;
select plan(3);

insert into quiz_questions (type, question_text, choices, correct_answer, is_active)
values ('mc', 'sample mc question', '["A","B","C","D"]'::jsonb, '1', true);

set local role anon;

select isnt(
  (select get_random_practice_question()),
  null,
  'anon은 get_random_practice_question을 호출할 수 있다'
);

select ok(
  (select get_random_practice_question()) ? 'question_text',
  '반환된 jsonb에 question_text 키가 있다'
);

select ok(
  not ((select get_random_practice_question()) ? 'correct_answer'),
  '반환된 jsonb에 correct_answer 키가 없다'
);

select * from finish();
rollback;
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

```bash
pnpm exec supabase test db
```

Expected: FAIL — `function get_random_practice_question() does not exist`.

- [ ] **Step 3: RPC 마이그레이션 작성**

`supabase/migrations/20260904090100_get_random_practice_question.sql`:

```sql
create or replace function get_random_practice_question() returns jsonb
language sql
security definer
set search_path = public, pg_temp
as $$
  select jsonb_build_object(
    'id', id, 'type', type, 'question_text', question_text, 'choices', choices
  )
  from quiz_questions
  where is_active
  order by random()
  limit 1;
$$;

grant execute on function get_random_practice_question() to anon;
```

- [ ] **Step 4: 테스트 재실행 → 통과 확인**

```bash
pnpm exec supabase test db
```

Expected: PASS.

- [ ] **Step 5: 커밋**

```bash
git add supabase/migrations/20260904090100_get_random_practice_question.sql supabase/tests/database/003_get_random_practice_question.sql
git commit -m "feat: 연습 모드용 get_random_practice_question RPC 추가"
```

---

### Task 4: RPC `start_quiz_attempt`

**Files:**

- Create: `supabase/tests/database/004_start_quiz_attempt.sql`
- Create: `supabase/migrations/20260904090200_start_quiz_attempt.sql`

**Interfaces:**

- Consumes: Task 2의 `quiz_questions`, `quiz_attempts`
- Produces: RPC `start_quiz_attempt(p_device_uuid text, p_fingerprint_hash text default null) returns jsonb` — `{already_submitted, attempt_id, score?, total?, questions?}`. Task 10의 `startQuizAttempt()`가 이 RPC를 호출.

- [ ] **Step 1: 실패하는 pgTAP 테스트 작성**

`supabase/tests/database/004_start_quiz_attempt.sql`:

```sql
begin;
select plan(5);

insert into quiz_questions (id, type, question_text, correct_answer, is_active)
select gen_random_uuid(), 'ox', 'q' || n, 'O', true
from generate_series(1, 12) as n;

insert into quiz_attempts (device_uuid, question_ids, status, score, total, submitted_at)
values ('device-already', '[]'::jsonb, 'submitted', 7, 10, now());

set local role anon;

select ok(
  (select (start_quiz_attempt('device-new', null) ->> 'already_submitted')::boolean) = false,
  '새 device_uuid로 시작하면 already_submitted는 false다'
);

select is(
  (select jsonb_array_length(start_quiz_attempt('device-new-2', null) -> 'questions')),
  10,
  '실제 테스트는 활성 문제 풀에서 10문항을 랜덤 서브셋으로 뽑는다'
);

select ok(
  not (
    select bool_or(q ? 'correct_answer')
    from jsonb_array_elements(start_quiz_attempt('device-new-3', null) -> 'questions') as q
  ),
  '반환된 문제 목록에는 correct_answer가 포함되지 않는다'
);

select ok(
  (select (start_quiz_attempt('device-already', null) ->> 'already_submitted')::boolean) = true,
  '이미 제출한 device_uuid로 다시 시작하면 already_submitted는 true다'
);

select is(
  (select (start_quiz_attempt('device-already', null) ->> 'score')::int),
  7,
  '이미 제출한 경우 이전 점수를 그대로 반환한다'
);

select * from finish();
rollback;
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

```bash
pnpm exec supabase test db
```

Expected: FAIL — `function start_quiz_attempt(text, text) does not exist`.

- [ ] **Step 3: RPC 마이그레이션 작성**

`supabase/migrations/20260904090200_start_quiz_attempt.sql`:

```sql
create or replace function start_quiz_attempt(
  p_device_uuid text,
  p_fingerprint_hash text default null
) returns jsonb
language plpgsql
security definer
set search_path = public, pg_temp
as $$
declare
  v_existing quiz_attempts;
  v_attempt_id uuid;
  v_question_ids jsonb;
  v_questions jsonb;
  v_ip inet;
begin
  select * into v_existing
  from quiz_attempts
  where status = 'submitted'
    and (
      device_uuid = p_device_uuid
      or (p_fingerprint_hash is not null and fingerprint_hash = p_fingerprint_hash)
    )
  order by submitted_at desc
  limit 1;

  if found then
    return jsonb_build_object(
      'already_submitted', true,
      'attempt_id', v_existing.id,
      'score', v_existing.score,
      'total', v_existing.total
    );
  end if;

  begin
    v_ip := (current_setting('request.headers', true)::json ->> 'x-forwarded-for')::inet;
  exception when others then
    v_ip := null;
  end;

  select jsonb_agg(id) into v_question_ids
  from (
    select id from quiz_questions
    where is_active
    order by random()
    limit 10
  ) sub;

  if v_question_ids is null or jsonb_array_length(v_question_ids) = 0 then
    raise exception 'no active quiz questions available';
  end if;

  insert into quiz_attempts (device_uuid, fingerprint_hash, ip, question_ids, status)
  values (p_device_uuid, p_fingerprint_hash, v_ip, v_question_ids, 'in_progress')
  returning id into v_attempt_id;

  select jsonb_agg(jsonb_build_object(
    'id', q.id, 'type', q.type, 'question_text', q.question_text, 'choices', q.choices
  )) into v_questions
  from quiz_questions q
  where q.id in (select jsonb_array_elements_text(v_question_ids)::uuid);

  return jsonb_build_object(
    'already_submitted', false,
    'attempt_id', v_attempt_id,
    'questions', v_questions
  );
end;
$$;

grant execute on function start_quiz_attempt(text, text) to anon;
```

- [ ] **Step 4: 테스트 재실행 → 통과 확인**

```bash
pnpm exec supabase test db
```

Expected: PASS.

- [ ] **Step 5: 커밋**

```bash
git add supabase/migrations/20260904090200_start_quiz_attempt.sql supabase/tests/database/004_start_quiz_attempt.sql
git commit -m "feat: 실제 테스트 시작용 start_quiz_attempt RPC 추가"
```

---

### Task 5: RPC `submit_quiz_attempt`

**Files:**

- Create: `supabase/tests/database/005_submit_quiz_attempt.sql`
- Create: `supabase/migrations/20260904090300_submit_quiz_attempt.sql`

**Interfaces:**

- Consumes: Task 2의 `quiz_questions`, `quiz_attempts`, Task 4가 만든 `in_progress` attempt
- Produces: RPC `submit_quiz_attempt(p_attempt_id uuid, p_device_uuid text, p_answers jsonb) returns jsonb` — `{score, total}`. Task 10의 `submitQuizAttempt()`가 이 RPC를 호출.

- [ ] **Step 1: 실패하는 pgTAP 테스트 작성**

`supabase/tests/database/005_submit_quiz_attempt.sql`:

```sql
begin;
select plan(5);

insert into quiz_questions (id, type, question_text, correct_answer, is_active)
values
  ('44444444-4444-4444-4444-444444444444', 'ox', 'q1', 'O', true),
  ('55555555-5555-5555-5555-555555555555', 'ox', 'q2', 'X', true);

insert into quiz_attempts (id, device_uuid, question_ids, status)
values (
  '66666666-6666-6666-6666-666666666666',
  'device-submit',
  '["44444444-4444-4444-4444-444444444444","55555555-5555-5555-5555-555555555555"]'::jsonb,
  'in_progress'
);

set local role anon;

select is(
  (select (submit_quiz_attempt(
    '66666666-6666-6666-6666-666666666666', 'device-submit',
    '{"44444444-4444-4444-4444-444444444444":"O","55555555-5555-5555-5555-555555555555":"O"}'::jsonb
  ) ->> 'score')::int),
  1,
  '2문항 중 1문항만 맞으면 score는 1이다'
);

prepare wrong_device as
  select submit_quiz_attempt(
    '66666666-6666-6666-6666-666666666666', 'someone-else', '{}'::jsonb
  );
select throws_ok(
  'wrong_device', null, 'attempt not found',
  '다른 device_uuid로는 제출할 수 없다'
);

prepare resubmit as
  select submit_quiz_attempt(
    '66666666-6666-6666-6666-666666666666', 'device-submit', '{}'::jsonb
  );
select throws_ok(
  'resubmit', null, 'attempt already submitted',
  '이미 제출된 attempt는 다시 제출할 수 없다'
);

prepare unknown_attempt as
  select submit_quiz_attempt(gen_random_uuid(), 'device-submit', '{}'::jsonb);
select throws_ok(
  'unknown_attempt', null, 'attempt not found',
  '존재하지 않는 attempt_id는 거부된다'
);

reset role;
select is(
  (select status from quiz_attempts where id = '66666666-6666-6666-6666-666666666666'),
  'submitted',
  '제출 후 status가 submitted로 바뀐다'
);

select * from finish();
rollback;
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

```bash
pnpm exec supabase test db
```

Expected: FAIL — `function submit_quiz_attempt(uuid, text, jsonb) does not exist`.

- [ ] **Step 3: RPC 마이그레이션 작성**

`supabase/migrations/20260904090300_submit_quiz_attempt.sql`:

```sql
create or replace function submit_quiz_attempt(
  p_attempt_id uuid,
  p_device_uuid text,
  p_answers jsonb
) returns jsonb
language plpgsql
security definer
set search_path = public, pg_temp
as $$
declare
  v_attempt quiz_attempts;
  v_score int := 0;
  v_total int := 0;
  v_question record;
  v_submitted_answer text;
begin
  select * into v_attempt
  from quiz_attempts
  where id = p_attempt_id and device_uuid = p_device_uuid;

  if not found then
    raise exception 'attempt not found';
  end if;

  if v_attempt.status = 'submitted' then
    raise exception 'attempt already submitted';
  end if;

  for v_question in
    select id, correct_answer
    from quiz_questions
    where id in (select jsonb_array_elements_text(v_attempt.question_ids)::uuid)
  loop
    v_total := v_total + 1;
    v_submitted_answer := p_answers ->> v_question.id::text;
    if v_submitted_answer is not null and v_submitted_answer = v_question.correct_answer then
      v_score := v_score + 1;
    end if;
  end loop;

  update quiz_attempts
  set answers = p_answers,
      score = v_score,
      total = v_total,
      status = 'submitted',
      submitted_at = now()
  where id = p_attempt_id;

  return jsonb_build_object('score', v_score, 'total', v_total);
end;
$$;

grant execute on function submit_quiz_attempt(uuid, text, jsonb) to anon;
```

- [ ] **Step 4: 테스트 재실행 → 통과 확인**

```bash
pnpm exec supabase test db
```

Expected: PASS.

- [ ] **Step 5: 커밋**

```bash
git add supabase/migrations/20260904090300_submit_quiz_attempt.sql supabase/tests/database/005_submit_quiz_attempt.sql
git commit -m "feat: 실제 테스트 제출/채점용 submit_quiz_attempt RPC 추가"
```

---

### Task 6: RPC `reveal_practice_answer`

**Files:**

- Create: `supabase/tests/database/006_reveal_practice_answer.sql`
- Create: `supabase/migrations/20260904090400_reveal_practice_answer.sql`

**Interfaces:**

- Consumes: Task 2의 `quiz_questions`
- Produces: RPC `reveal_practice_answer(p_question_id uuid) returns jsonb` — `{correct_answer, explanation}`. Task 10의 `revealPracticeAnswer()`가 이 RPC를 호출.

- [ ] **Step 1: 실패하는 pgTAP 테스트 작성**

`supabase/tests/database/006_reveal_practice_answer.sql`:

```sql
begin;
select plan(3);

insert into quiz_questions (id, type, question_text, correct_answer, explanation, is_active)
values ('77777777-7777-7777-7777-777777777777', 'ox', 'q', 'X', 'test explanation', true);

insert into quiz_questions (id, type, question_text, correct_answer, is_active)
values ('88888888-8888-8888-8888-888888888888', 'ox', 'inactive', 'O', false);

set local role anon;

select is(
  (select reveal_practice_answer('77777777-7777-7777-7777-777777777777') ->> 'correct_answer'),
  'X',
  '활성 문제의 정답을 정확히 반환한다'
);

select is(
  (select reveal_practice_answer('77777777-7777-7777-7777-777777777777') ->> 'explanation'),
  'test explanation',
  '해설도 함께 반환한다'
);

prepare inactive_reveal as
  select reveal_practice_answer('88888888-8888-8888-8888-888888888888');
select throws_ok(
  'inactive_reveal', null, 'question not found',
  '비활성 문제는 정답을 공개하지 않는다'
);

select * from finish();
rollback;
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

```bash
pnpm exec supabase test db
```

Expected: FAIL — `function reveal_practice_answer(uuid) does not exist`.

- [ ] **Step 3: RPC 마이그레이션 작성**

`supabase/migrations/20260904090400_reveal_practice_answer.sql`:

```sql
create or replace function reveal_practice_answer(p_question_id uuid) returns jsonb
language plpgsql
security definer
set search_path = public, pg_temp
as $$
declare
  v_question quiz_questions;
begin
  select * into v_question from quiz_questions where id = p_question_id and is_active;

  if not found then
    raise exception 'question not found';
  end if;

  return jsonb_build_object(
    'correct_answer', v_question.correct_answer,
    'explanation', v_question.explanation
  );
end;
$$;

grant execute on function reveal_practice_answer(uuid) to anon;
```

- [ ] **Step 4: 테스트 재실행 → 통과 확인**

```bash
pnpm exec supabase test db
```

Expected: PASS — pgTAP 테스트 6개 파일 전부 통과.

- [ ] **Step 5: 커밋**

```bash
git add supabase/migrations/20260904090400_reveal_practice_answer.sql supabase/tests/database/006_reveal_practice_answer.sql
git commit -m "feat: 연습 모드 정답 공개용 reveal_practice_answer RPC 추가"
```

---

### Task 7: Vitest 셋업 + Supabase 클라이언트

**Files:**

- Modify: `package.json`
- Create: `vitest.config.ts`
- Create: `src/lib/supabase/client.ts`
- Test: `src/lib/supabase/client.test.ts`

**Interfaces:**

- Consumes: Task 1의 `.env.local` (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUB_KEY`)
- Produces: `export const supabase: SupabaseClient` — Task 10의 `src/features/quiz/lib/api.ts`가 이 클라이언트를 소비.

- [ ] **Step 1: 의존성 설치**

```bash
pnpm add @supabase/supabase-js
pnpm add -D vitest jsdom
```

- [ ] **Step 2: `package.json`에 test 스크립트 추가**

```json
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "pages:deploy": "next build && wrangler pages deploy out"
  },
```

- [ ] **Step 3: `vitest.config.ts` 작성**

```ts
import { defineConfig } from "vitest/config";
import path from "node:path";

export default defineConfig({
  test: {
    environment: "jsdom",
    include: ["src/**/*.test.ts"],
  },
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
});
```

- [ ] **Step 4: 실패하는 테스트 작성**

`src/lib/supabase/client.test.ts`:

```ts
import { describe, it, expect, beforeEach, vi } from "vitest";

describe("supabase client", () => {
  const ORIGINAL_ENV = { ...process.env };

  beforeEach(() => {
    vi.resetModules();
    process.env = { ...ORIGINAL_ENV };
  });

  it("throws when NEXT_PUBLIC_SUPABASE_URL is missing", async () => {
    delete process.env.NEXT_PUBLIC_SUPABASE_URL;
    process.env.NEXT_PUBLIC_SUPABASE_PUB_KEY = "anon-key";

    await expect(import("./client")).rejects.toThrow(
      /NEXT_PUBLIC_SUPABASE_URL/,
    );
  });

  it("creates a client when both env vars are present", async () => {
    process.env.NEXT_PUBLIC_SUPABASE_URL = "https://example.supabase.co";
    process.env.NEXT_PUBLIC_SUPABASE_PUB_KEY = "anon-key";

    const { supabase } = await import("./client");
    expect(supabase).toBeDefined();
  });
});
```

- [ ] **Step 5: 테스트 실행 → 실패 확인**

```bash
pnpm test -- src/lib/supabase/client.test.ts
```

Expected: FAIL — `./client` 모듈이 없음.

- [ ] **Step 6: 구현**

`src/lib/supabase/client.ts`:

```ts
import { createClient } from "@supabase/supabase-js";

const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL;
const supabasePubKey = process.env.NEXT_PUBLIC_SUPABASE_PUB_KEY;

if (!supabaseUrl || !supabasePubKey) {
  throw new Error(
    "NEXT_PUBLIC_SUPABASE_URL / NEXT_PUBLIC_SUPABASE_PUB_KEY 환경변수가 필요합니다.",
  );
}

export const supabase = createClient(supabaseUrl, supabasePubKey);
```

- [ ] **Step 7: 테스트 재실행 → 통과 확인**

```bash
pnpm test -- src/lib/supabase/client.test.ts
```

Expected: PASS.

- [ ] **Step 8: 커밋**

```bash
git add package.json pnpm-lock.yaml vitest.config.ts src/lib/supabase/client.ts src/lib/supabase/client.test.ts
git commit -m "feat: Vitest 도입 및 Supabase 클라이언트 초기화"
```

---

### Task 8: `device-id.ts` (기기 UUID 발급/저장)

**Files:**

- Create: `src/features/quiz/lib/device-id.ts`
- Test: `src/features/quiz/lib/device-id.test.ts`

**Interfaces:**

- Consumes: 없음 (브라우저 `localStorage`/`document.cookie`, `crypto.randomUUID`)
- Produces: `export function getDeviceId(): string` — Task 12의 `QuizAttempt` 컴포넌트가 소비.

- [ ] **Step 1: 실패하는 테스트 작성**

`src/features/quiz/lib/device-id.test.ts`:

```ts
import { describe, it, expect, beforeEach } from "vitest";
import { getDeviceId } from "./device-id";

function clearDeviceCookie() {
  document.cookie = "ztmy_quiz_device_id=; path=/; max-age=0";
}

describe("getDeviceId", () => {
  beforeEach(() => {
    window.localStorage.clear();
    clearDeviceCookie();
  });

  it("generates a new UUID when nothing is stored", () => {
    const id = getDeviceId();
    expect(id).toMatch(/^[0-9a-f-]{36}$/);
  });

  it("returns the same id on repeated calls (persisted via localStorage)", () => {
    const first = getDeviceId();
    const second = getDeviceId();
    expect(second).toBe(first);
  });

  it("falls back to the cookie value when localStorage is empty, and backfills it", () => {
    const id = getDeviceId();
    window.localStorage.clear();

    const recovered = getDeviceId();
    expect(recovered).toBe(id);
    expect(window.localStorage.getItem("ztmy_quiz_device_id")).toBe(id);
  });
});
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

```bash
pnpm test -- src/features/quiz/lib/device-id.test.ts
```

Expected: FAIL — `./device-id` 모듈이 없음.

- [ ] **Step 3: 구현**

`src/features/quiz/lib/device-id.ts`:

```ts
const STORAGE_KEY = "ztmy_quiz_device_id";
const COOKIE_MAX_AGE_SECONDS = 60 * 60 * 24 * 365 * 2;

function readCookie(name: string): string | null {
  const match = document.cookie.match(new RegExp(`(?:^|; )${name}=([^;]*)`));
  return match ? decodeURIComponent(match[1]) : null;
}

function writeCookie(name: string, value: string): void {
  document.cookie = `${name}=${encodeURIComponent(value)}; path=/; max-age=${COOKIE_MAX_AGE_SECONDS}; samesite=lax`;
}

export function getDeviceId(): string {
  const fromStorage = window.localStorage.getItem(STORAGE_KEY);
  const fromCookie = readCookie(STORAGE_KEY);
  const deviceId = fromStorage ?? fromCookie ?? crypto.randomUUID();

  if (fromStorage !== deviceId) {
    window.localStorage.setItem(STORAGE_KEY, deviceId);
  }
  if (fromCookie !== deviceId) {
    writeCookie(STORAGE_KEY, deviceId);
  }

  return deviceId;
}
```

- [ ] **Step 4: 테스트 재실행 → 통과 확인**

```bash
pnpm test -- src/features/quiz/lib/device-id.test.ts
```

Expected: PASS.

- [ ] **Step 5: 커밋**

```bash
git add src/features/quiz/lib/device-id.ts src/features/quiz/lib/device-id.test.ts
git commit -m "feat: 기기 식별용 device-id 유틸 추가"
```

---

### Task 9: `fingerprint.ts` (FingerprintJS 연동)

**Files:**

- Create: `src/features/quiz/lib/fingerprint.ts`
- Test: `src/features/quiz/lib/fingerprint.test.ts`

**Interfaces:**

- Consumes: `@fingerprintjs/fingerprintjs`
- Produces: `export function getFingerprint(): Promise<string>` — Task 12의 `QuizAttempt` 컴포넌트가 소비.

- [ ] **Step 1: 의존성 설치**

```bash
pnpm add @fingerprintjs/fingerprintjs
```

- [ ] **Step 2: 실패하는 테스트 작성**

`src/features/quiz/lib/fingerprint.test.ts`:

```ts
import { describe, it, expect, vi, beforeEach } from "vitest";

vi.mock("@fingerprintjs/fingerprintjs", () => ({
  default: { load: vi.fn() },
}));

describe("getFingerprint", () => {
  beforeEach(() => {
    vi.resetModules();
  });

  it("returns the visitorId from the loaded agent", async () => {
    const FingerprintJS = (await import("@fingerprintjs/fingerprintjs"))
      .default;
    vi.mocked(FingerprintJS.load).mockResolvedValue({
      get: () => Promise.resolve({ visitorId: "abc123" }),
    } as never);

    const { getFingerprint } = await import("./fingerprint");
    await expect(getFingerprint()).resolves.toBe("abc123");
  });

  it("caches the agent load across repeated calls", async () => {
    const FingerprintJS = (await import("@fingerprintjs/fingerprintjs"))
      .default;
    const getMock = vi.fn().mockResolvedValue({ visitorId: "abc123" });
    vi.mocked(FingerprintJS.load).mockResolvedValue({ get: getMock } as never);

    const { getFingerprint } = await import("./fingerprint");
    await getFingerprint();
    await getFingerprint();

    expect(FingerprintJS.load).toHaveBeenCalledTimes(1);
  });
});
```

- [ ] **Step 3: 테스트 실행 → 실패 확인**

```bash
pnpm test -- src/features/quiz/lib/fingerprint.test.ts
```

Expected: FAIL — `./fingerprint` 모듈이 없음.

- [ ] **Step 4: 구현**

`src/features/quiz/lib/fingerprint.ts`:

```ts
import FingerprintJS from "@fingerprintjs/fingerprintjs";

let cachedVisitorId: Promise<string> | null = null;

export function getFingerprint(): Promise<string> {
  if (!cachedVisitorId) {
    cachedVisitorId = FingerprintJS.load()
      .then((agent) => agent.get())
      .then((result) => result.visitorId);
  }
  return cachedVisitorId;
}
```

- [ ] **Step 5: 테스트 재실행 → 통과 확인**

```bash
pnpm test -- src/features/quiz/lib/fingerprint.test.ts
```

Expected: PASS.

- [ ] **Step 6: 커밋**

```bash
git add package.json pnpm-lock.yaml src/features/quiz/lib/fingerprint.ts src/features/quiz/lib/fingerprint.test.ts
git commit -m "feat: FingerprintJS 기반 기기 지문 유틸 추가"
```

---

### Task 10: 퀴즈 도메인 타입 + API 래퍼

**Files:**

- Create: `src/features/quiz/lib/types.ts`
- Create: `src/features/quiz/lib/api.ts`
- Test: `src/features/quiz/lib/api.test.ts`

**Interfaces:**

- Consumes: Task 7의 `supabase` 클라이언트, Task 3~6의 RPC 4종
- Produces: `getRandomPracticeQuestion()`, `revealPracticeAnswer(questionId)`, `startQuizAttempt(deviceUuid, fingerprintHash)`, `submitQuizAttempt(attemptId, deviceUuid, answers)` — Task 11(연습 UI), Task 12(실제 테스트 UI)가 소비.

- [ ] **Step 1: 타입 정의**

`src/features/quiz/lib/types.ts`:

```ts
export type QuestionType = "mc" | "ox";

export interface PracticeQuestion {
  id: string;
  type: QuestionType;
  question_text: string;
  choices: string[] | null;
}

export interface RevealAnswerResult {
  correct_answer: string;
  explanation: string | null;
}

export interface StartAttemptResult {
  already_submitted: boolean;
  attempt_id: string;
  score?: number;
  total?: number;
  questions?: PracticeQuestion[];
}

export interface SubmitAttemptResult {
  score: number;
  total: number;
}
```

- [ ] **Step 2: 실패하는 테스트 작성**

`src/features/quiz/lib/api.test.ts`:

```ts
import { describe, it, expect, vi, beforeEach } from "vitest";

vi.mock("@/lib/supabase/client", () => ({
  supabase: { rpc: vi.fn() },
}));

import { supabase } from "@/lib/supabase/client";
import {
  getRandomPracticeQuestion,
  revealPracticeAnswer,
  startQuizAttempt,
  submitQuizAttempt,
} from "./api";

describe("quiz api", () => {
  beforeEach(() => {
    vi.mocked(supabase.rpc).mockReset();
  });

  it("getRandomPracticeQuestion calls the RPC and returns data", async () => {
    vi.mocked(supabase.rpc).mockResolvedValue({
      data: { id: "q1", type: "ox", question_text: "?", choices: null },
      error: null,
    } as never);

    const result = await getRandomPracticeQuestion();

    expect(supabase.rpc).toHaveBeenCalledWith("get_random_practice_question");
    expect(result.id).toBe("q1");
  });

  it("revealPracticeAnswer passes the question id", async () => {
    vi.mocked(supabase.rpc).mockResolvedValue({
      data: { correct_answer: "O", explanation: "설명" },
      error: null,
    } as never);

    await revealPracticeAnswer("q1");

    expect(supabase.rpc).toHaveBeenCalledWith("reveal_practice_answer", {
      p_question_id: "q1",
    });
  });

  it("startQuizAttempt passes device id and fingerprint", async () => {
    vi.mocked(supabase.rpc).mockResolvedValue({
      data: { already_submitted: false, attempt_id: "a1", questions: [] },
      error: null,
    } as never);

    await startQuizAttempt("device-1", "fp-1");

    expect(supabase.rpc).toHaveBeenCalledWith("start_quiz_attempt", {
      p_device_uuid: "device-1",
      p_fingerprint_hash: "fp-1",
    });
  });

  it("submitQuizAttempt passes attempt id, device id and answers", async () => {
    vi.mocked(supabase.rpc).mockResolvedValue({
      data: { score: 1, total: 2 },
      error: null,
    } as never);

    await submitQuizAttempt("a1", "device-1", { q1: "O" });

    expect(supabase.rpc).toHaveBeenCalledWith("submit_quiz_attempt", {
      p_attempt_id: "a1",
      p_device_uuid: "device-1",
      p_answers: { q1: "O" },
    });
  });

  it("throws when Supabase returns an error", async () => {
    vi.mocked(supabase.rpc).mockResolvedValue({
      data: null,
      error: new Error("boom"),
    } as never);

    await expect(getRandomPracticeQuestion()).rejects.toThrow("boom");
  });
});
```

- [ ] **Step 3: 테스트 실행 → 실패 확인**

```bash
pnpm test -- src/features/quiz/lib/api.test.ts
```

Expected: FAIL — `./api` 모듈이 없음.

- [ ] **Step 4: 구현**

`src/features/quiz/lib/api.ts`:

```ts
import { supabase } from "@/lib/supabase/client";
import type {
  PracticeQuestion,
  RevealAnswerResult,
  StartAttemptResult,
  SubmitAttemptResult,
} from "./types";

export async function getRandomPracticeQuestion(): Promise<PracticeQuestion> {
  const { data, error } = await supabase.rpc("get_random_practice_question");
  if (error) throw error;
  return data as PracticeQuestion;
}

export async function revealPracticeAnswer(
  questionId: string,
): Promise<RevealAnswerResult> {
  const { data, error } = await supabase.rpc("reveal_practice_answer", {
    p_question_id: questionId,
  });
  if (error) throw error;
  return data as RevealAnswerResult;
}

export async function startQuizAttempt(
  deviceUuid: string,
  fingerprintHash: string | null,
): Promise<StartAttemptResult> {
  const { data, error } = await supabase.rpc("start_quiz_attempt", {
    p_device_uuid: deviceUuid,
    p_fingerprint_hash: fingerprintHash,
  });
  if (error) throw error;
  return data as StartAttemptResult;
}

export async function submitQuizAttempt(
  attemptId: string,
  deviceUuid: string,
  answers: Record<string, string>,
): Promise<SubmitAttemptResult> {
  const { data, error } = await supabase.rpc("submit_quiz_attempt", {
    p_attempt_id: attemptId,
    p_device_uuid: deviceUuid,
    p_answers: answers,
  });
  if (error) throw error;
  return data as SubmitAttemptResult;
}
```

- [ ] **Step 5: 테스트 재실행 → 통과 확인**

```bash
pnpm test -- src/features/quiz/lib/api.test.ts
```

Expected: PASS.

- [ ] **Step 6: 커밋**

```bash
git add src/features/quiz/lib/types.ts src/features/quiz/lib/api.ts src/features/quiz/lib/api.test.ts
git commit -m "feat: 퀴즈 도메인 타입 및 Supabase RPC API 래퍼 추가"
```

---

### Task 11: 연습 모드 UI

**Files:**

- Create: `src/features/quiz/components/QuizPractice.tsx`
- Create: `src/app/(pages)/quiz/practice/page.tsx`

**Interfaces:**

- Consumes: Task 10의 `getRandomPracticeQuestion`, `revealPracticeAnswer`, `PracticeQuestion`, `RevealAnswerResult`
- Produces: `/quiz/practice` 페이지 (수동 검증 대상 — 이 태스크는 자동 테스트를 두지 않음, Supabase 호출 mocking 없이 컴포넌트를 테스트하려면 범위가 커져 이번 플랜에서는 제외하기로 결정함)

- [ ] **Step 1: 컴포넌트 구현**

`src/features/quiz/components/QuizPractice.tsx`:

```tsx
"use client";

import { useCallback, useEffect, useState } from "react";
import { cn } from "@/lib/utils";
import {
  getRandomPracticeQuestion,
  revealPracticeAnswer,
} from "@/features/quiz/lib/api";
import type {
  PracticeQuestion,
  RevealAnswerResult,
} from "@/features/quiz/lib/types";

type Phase = "loading" | "answering" | "revealed" | "error";

export function QuizPractice() {
  const [phase, setPhase] = useState<Phase>("loading");
  const [question, setQuestion] = useState<PracticeQuestion | null>(null);
  const [selected, setSelected] = useState<string | null>(null);
  const [reveal, setReveal] = useState<RevealAnswerResult | null>(null);

  const loadQuestion = useCallback(() => {
    setPhase("loading");
    setSelected(null);
    setReveal(null);
    getRandomPracticeQuestion()
      .then((q) => {
        setQuestion(q);
        setPhase("answering");
      })
      .catch(() => setPhase("error"));
  }, []);

  useEffect(() => {
    loadQuestion();
  }, [loadQuestion]);

  if (phase === "loading") {
    return <p className="text-white/60">문제를 불러오는 중...</p>;
  }

  if (phase === "error") {
    return <p className="text-ztmy-pink">문제를 불러오지 못했습니다.</p>;
  }

  if (!question) return null;

  const options =
    question.type === "ox" ? ["O", "X"] : (question.choices ?? []);

  const handleSelect = (optionValue: string) => {
    if (phase !== "answering") return;
    setSelected(optionValue);
    revealPracticeAnswer(question.id)
      .then((result) => {
        setReveal(result);
        setPhase("revealed");
      })
      .catch(() => setPhase("error"));
  };

  return (
    <div className="flex flex-col gap-4">
      <p className="text-lg font-bold text-white">{question.question_text}</p>

      <div className="flex flex-wrap gap-2">
        {options.map((option, index) => {
          const optionValue = question.type === "mc" ? String(index) : option;
          const isSelected = selected === optionValue;
          const isCorrect = reveal?.correct_answer === optionValue;

          return (
            <button
              key={option}
              type="button"
              disabled={phase === "revealed"}
              onClick={() => handleSelect(optionValue)}
              className={cn(
                "rounded-sm border border-white/20 px-4 py-2 text-sm text-white transition-colors",
                "hover:border-ztmy-purple",
                phase === "revealed" &&
                  isCorrect &&
                  "border-ztmy-purple bg-ztmy-purple/30",
                phase === "revealed" &&
                  isSelected &&
                  !isCorrect &&
                  "border-ztmy-pink bg-ztmy-pink/20",
              )}
            >
              {option}
            </button>
          );
        })}
      </div>

      {phase === "revealed" && reveal && (
        <div className="rounded-sm border border-white/10 bg-black/40 p-3 text-sm text-white/80">
          <p>
            정답:{" "}
            <span className="text-ztmy-purple font-bold">
              {reveal.correct_answer}
            </span>
          </p>
          {reveal.explanation && (
            <p className="mt-1 text-white/60">{reveal.explanation}</p>
          )}
        </div>
      )}

      {phase === "revealed" && (
        <button
          type="button"
          onClick={loadQuestion}
          className="bg-ztmy-purple w-fit rounded-sm px-4 py-2 text-sm font-bold text-white"
        >
          다음 문제
        </button>
      )}
    </div>
  );
}
```

- [ ] **Step 2: 페이지 구현**

`src/app/(pages)/quiz/practice/page.tsx`:

```tsx
import { QuizPractice } from "@/features/quiz/components/QuizPractice";

export default function QuizPracticePage() {
  return (
    <main className="mx-auto flex max-w-2xl flex-col gap-6 px-4 py-8">
      <h1 className="text-2xl font-bold text-white">퀴즈 연습</h1>
      <QuizPractice />
    </main>
  );
}
```

- [ ] **Step 3: 수동 검증용 샘플 문제 삽입**

로컬 Supabase Studio(`pnpm exec supabase status`로 나오는 Studio URL, 기본 `http://127.0.0.1:54323`) SQL Editor에서:

```sql
insert into quiz_questions (type, question_text, choices, correct_answer, explanation, is_active)
values ('ox', '[샘플] 이것은 연습 모드 테스트용 문제입니다', null, 'O', '샘플 해설입니다', true);
```

- [ ] **Step 4: 수동 검증**

```bash
pnpm dev
```

`http://localhost:3000/quiz/practice` 접속 후 확인:

- 문제와 O/X 버튼이 렌더링된다.
- 버튼 클릭 시 정답/해설이 즉시 표시되고, 정답 버튼은 보라색, 오답으로 고른 버튼은 핑크색으로 표시된다.
- "다음 문제" 클릭 시 같은 문제(현재 DB에 1개뿐이므로)가 다시 로딩되고 초기 상태로 돌아간다.

- [ ] **Step 5: 커밋**

```bash
git add src/features/quiz/components/QuizPractice.tsx "src/app/(pages)/quiz/practice/page.tsx"
git commit -m "feat: 퀴즈 연습 모드 UI 추가"
```

---

### Task 12: 실제 테스트 UI

**Files:**

- Create: `src/features/quiz/components/QuizAttempt.tsx`
- Create: `src/app/(pages)/quiz/test/page.tsx`

**Interfaces:**

- Consumes: Task 8의 `getDeviceId`, Task 9의 `getFingerprint`, Task 10의 `startQuizAttempt`/`submitQuizAttempt`/`PracticeQuestion`
- Produces: `/quiz/test` 페이지 (수동 검증 대상 — Task 11과 동일한 이유로 자동 테스트 없음)

- [ ] **Step 1: 컴포넌트 구현**

`src/features/quiz/components/QuizAttempt.tsx`:

```tsx
"use client";

import { useEffect, useState } from "react";
import { cn } from "@/lib/utils";
import { getDeviceId } from "@/features/quiz/lib/device-id";
import { getFingerprint } from "@/features/quiz/lib/fingerprint";
import { startQuizAttempt, submitQuizAttempt } from "@/features/quiz/lib/api";
import type { PracticeQuestion } from "@/features/quiz/lib/types";

type Phase = "loading" | "answering" | "submitting" | "result" | "error";

export function QuizAttempt() {
  const [phase, setPhase] = useState<Phase>("loading");
  const [attemptId, setAttemptId] = useState<string | null>(null);
  const [questions, setQuestions] = useState<PracticeQuestion[]>([]);
  const [answers, setAnswers] = useState<Record<string, string>>({});
  const [result, setResult] = useState<{ score: number; total: number } | null>(
    null,
  );

  useEffect(() => {
    const deviceId = getDeviceId();

    getFingerprint()
      .catch(() => null)
      .then((fingerprint) => startQuizAttempt(deviceId, fingerprint ?? null))
      .then((res) => {
        if (res.already_submitted) {
          setResult({ score: res.score ?? 0, total: res.total ?? 0 });
          setPhase("result");
          return;
        }
        setAttemptId(res.attempt_id);
        setQuestions(res.questions ?? []);
        setPhase("answering");
      })
      .catch(() => setPhase("error"));
  }, []);

  const handleAnswer = (questionId: string, value: string) => {
    setAnswers((prev) => ({ ...prev, [questionId]: value }));
  };

  const handleSubmit = () => {
    if (!attemptId) return;
    setPhase("submitting");
    submitQuizAttempt(attemptId, getDeviceId(), answers)
      .then((res) => {
        setResult(res);
        setPhase("result");
      })
      .catch(() => setPhase("error"));
  };

  const allAnswered =
    questions.length > 0 && questions.every((q) => answers[q.id] !== undefined);

  if (phase === "loading") {
    return <p className="text-white/60">응시 정보를 확인하는 중...</p>;
  }

  if (phase === "error") {
    return <p className="text-ztmy-pink">문제를 불러오지 못했습니다.</p>;
  }

  if (phase === "result" && result) {
    return (
      <div className="flex flex-col items-center gap-2 rounded-sm border border-white/10 bg-black/40 p-6">
        <p className="text-sm text-white/60">결과</p>
        <p className="text-ztmy-purple text-3xl font-bold">
          {result.score} / {result.total}
        </p>
      </div>
    );
  }

  return (
    <div className="flex flex-col gap-6">
      {questions.map((question, qIndex) => {
        const options =
          question.type === "ox" ? ["O", "X"] : (question.choices ?? []);

        return (
          <div key={question.id} className="flex flex-col gap-2">
            <p className="font-bold text-white">
              {qIndex + 1}. {question.question_text}
            </p>
            <div className="flex flex-wrap gap-2">
              {options.map((option, index) => {
                const optionValue =
                  question.type === "mc" ? String(index) : option;
                const isSelected = answers[question.id] === optionValue;

                return (
                  <button
                    key={option}
                    type="button"
                    onClick={() => handleAnswer(question.id, optionValue)}
                    className={cn(
                      "rounded-sm border px-4 py-2 text-sm text-white",
                      isSelected
                        ? "border-ztmy-purple bg-ztmy-purple/30"
                        : "hover:border-ztmy-purple border-white/20",
                    )}
                  >
                    {option}
                  </button>
                );
              })}
            </div>
          </div>
        );
      })}

      <button
        type="button"
        disabled={!allAnswered || phase === "submitting"}
        onClick={handleSubmit}
        className="bg-ztmy-purple w-fit rounded-sm px-6 py-2 font-bold text-white disabled:opacity-40"
      >
        {phase === "submitting" ? "제출 중..." : "제출하기"}
      </button>
    </div>
  );
}
```

- [ ] **Step 2: 페이지 구현**

`src/app/(pages)/quiz/test/page.tsx`:

```tsx
import { QuizAttempt } from "@/features/quiz/components/QuizAttempt";

export default function QuizTestPage() {
  return (
    <main className="mx-auto flex max-w-2xl flex-col gap-6 px-4 py-8">
      <h1 className="text-2xl font-bold text-white">실제 테스트</h1>
      <QuizAttempt />
    </main>
  );
}
```

- [ ] **Step 3: 수동 검증용 샘플 문제 10개 이상 삽입**

로컬 Supabase Studio SQL Editor에서:

```sql
insert into quiz_questions (type, question_text, choices, correct_answer, is_active)
select
  'ox',
  '[샘플] 실제 테스트용 문제 ' || n,
  null,
  case when n % 2 = 0 then 'O' else 'X' end,
  true
from generate_series(1, 12) as n;
```

- [ ] **Step 4: 수동 검증**

```bash
pnpm dev
```

`http://localhost:3000/quiz/test` 접속 후 확인:

- 문제 10개가 한 번에 렌더링된다 (풀 12문항 중 랜덤 10문항).
- 전부 답하기 전에는 "제출하기" 버튼이 비활성화된다.
- 전부 답한 뒤 제출하면 점수(`n / 10`)가 표시된다.
- 페이지를 새로고침하면 문제를 다시 풀지 않고 방금 받은 점수가 바로 표시된다 (기기당 1회 제한 동작 확인).
- 브라우저 개발자 도구 Network 탭에서 `start_quiz_attempt`/`submit_quiz_attempt` 응답에 `correct_answer` 필드가 없는지 확인한다.

- [ ] **Step 5: 커밋**

```bash
git add src/features/quiz/components/QuizAttempt.tsx "src/app/(pages)/quiz/test/page.tsx"
git commit -m "feat: 퀴즈 실제 테스트 UI 추가"
```

---

### Task 13: 퀴즈 진입 페이지

**Files:**

- Create: `src/app/(pages)/quiz/page.tsx`

**Interfaces:**

- Consumes: 없음 (단순 네비게이션 페이지)
- Produces: `/quiz` 진입점

- [ ] **Step 1: 구현**

`src/app/(pages)/quiz/page.tsx`:

```tsx
import Link from "next/link";

export default function QuizHomePage() {
  return (
    <main className="mx-auto flex max-w-2xl flex-col gap-6 px-4 py-8">
      <h1 className="text-2xl font-bold text-white">퀴즈</h1>
      <div className="flex flex-col gap-3">
        <Link
          href="/quiz/practice"
          className="hover:border-ztmy-purple rounded-sm border border-white/20 px-4 py-3 text-white"
        >
          연습 문제 풀기
        </Link>
        <Link
          href="/quiz/test"
          className="hover:border-ztmy-purple rounded-sm border border-white/20 px-4 py-3 text-white"
        >
          실제 테스트 응시 (기기당 1회)
        </Link>
      </div>
    </main>
  );
}
```

- [ ] **Step 2: 전체 플로우 최종 수동 검증**

```bash
pnpm test
pnpm lint
pnpm format:check
pnpm exec supabase test db
```

Expected: 전부 통과. 이어서 `/quiz` → 연습 → 실제 테스트 순서로 브라우저에서 한 번 더 클릭해보고 Task 11/12의 검증 항목이 모두 유지되는지 확인.

- [ ] **Step 3: 커밋**

```bash
git add "src/app/(pages)/quiz/page.tsx"
git commit -m "feat: 퀴즈 진입 페이지 추가"
```

---

## 이후 플랜

문제 CRUD를 SQL Editor 대신 관리자 페이지에서 하고 싶다면, 별도 플랜(`docs/superpowers/plans/2026-09-04-quiz-admin.md`, 스펙 §8 기반)을 이어서 작성한다 — `admins` 테이블, Supabase Auth 로그인 게이트, 문제 CRUD 화면, 응시 기록 조회/리셋, Cloudflare Access 설정을 다룬다.
