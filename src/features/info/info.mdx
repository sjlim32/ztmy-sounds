import { ZoomableImage } from "@/components/ZoomableImage";
import {
  EventDetails,
  ImageLabel,
} from "@/features/info/components/EventDetails";
import { TableOfContents } from "@/features/info/components/TableOfContents";
import { groupImagesBySection } from "@/features/info/lib/group-images";
import { INFORMATION } from "@/features/info/info";

export const sections = groupImagesBySection(INFORMATION.img.sub);

<div className="mb-1 flex w-full justify-center">
  <ZoomableImage
    src={INFORMATION.img.main}
    alt={INFORMATION.name}
    thumbnailWidth={300}
    thumbnailCrop="top"
  />
</div>

<div className="tablet:mb-6 mb-3 text-center text-sm font-semibold text-white/60">
  {"모든 이미지들은 클릭 시 확대됩니다."}
</div>

<TableOfContents />

<EventDetails event={INFORMATION} />

<ImageLabel id="event-photos" label="공연 정보" />

<section className="flex flex-col gap-3">
  <div className="tablet:flex-row flex flex-col items-center justify-center gap-3">
    {sections["event-photos"].map((img) => (
      <ZoomableImage
        key={img.asset}
        src={img.asset}
        alt={img.name}
        thumbnailWidth={300}
        thumbnailCrop="top"
      />
    ))}
  </div>
  
    <div className="tablet:grid tablet:grid-cols-3 flex flex-col items-center gap-3">
    {sections["event-details"].map((img) => (
      <ZoomableImage
        key={img.asset}
        src={img.asset}
        alt={img.name}
        thumbnailWidth={300}
        thumbnailCrop="top"
      />
    ))}
  </div>

<ImageLabel id="notice" label="유의 사항" />

<div className="tablet:grid tablet:grid-cols-3 flex flex-col items-center gap-3">
  {sections["notice"].map((img) => (
    <ZoomableImage
      key={img.asset}
      src={img.asset}
      alt={img.name}
      thumbnailWidth={500}
      thumbnailCrop="top"
    />
  ))}
</div>

<ImageLabel id="goods" label="굿즈" />

<div className="tablet:grid tablet:grid-cols-2 flex flex-col items-center gap-3">
  {sections["goods"].map((img) => (
    <ZoomableImage
      key={img.asset}
      src={img.asset}
      alt={img.name}
      thumbnailWidth={500}
      thumbnailCrop="top"
    />
  ))}
</div>

<ImageLabel id="fnb" label="F & B" />

  <div className="tablet:grid tablet:grid-cols-2 flex flex-col items-center gap-3">
    {sections["fnb"].map((img) => (
      <ZoomableImage
        key={img.asset}
        src={img.asset}
        alt={img.name}
        thumbnailWidth={500}
        thumbnailCrop="top"
      />
    ))}
  </div>
</section>
